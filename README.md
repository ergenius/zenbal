# gembal
GEM BAsh Library

The GEM BAsh Library (gembal) is a small bash script that acts as a library for other bash scripts.

It provides useful functions that makes the lives of people using shell scripts easier.
